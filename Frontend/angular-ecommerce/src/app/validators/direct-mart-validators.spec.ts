import { DirectMartShopValidators } from './direct-mart-validators';

describe('DirectMartShopValidators', () => {
  it('should create an instance', () => {
    expect(new DirectMartShopValidators()).toBeTruthy();
  });
});
